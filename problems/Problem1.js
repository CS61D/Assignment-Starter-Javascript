// Problem 1
// function must have the name printIndex

export default printIndex; // Uncomment this line after writing the function

function printIndex(ind, arr) {
  return `Value at index ${ind} is ${arr[ind]}`;
}
